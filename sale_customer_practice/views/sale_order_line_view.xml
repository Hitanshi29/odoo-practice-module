<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="show_quntatity" model="ir.ui.view">
            <field name="name">sale.order.list.inherit</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_quotation_tree_with_onboarding"/>
            <field name="arch" type="xml">
                <!-- <xpath expr="//field[@name='name']" position="after">
                    <field name="quantity_count_order" readonly="1"/>
                </xpath> -->
                <field name="name" position="after">
                    <field name="quantity_count_order" readonly="1"/>
                </field>
            </field>
        </record>


        <record id="sale_order_filter_today" model="ir.ui.view">
            <field name="name">sale.order.search.inherit</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_sales_order_filter"/>
            <field name="arch" type="xml">
                <xpath expr="//filter[@name='my_sale_orders_filter']" position="after">
                    <filter name="today" string="Today" 
                        domain="[('date_order','&gt;', (context_today()).strftime('%%Y-%%m-%%d 00:00:00')),('date_order','&lt;', 
                        (context_today()).strftime('%%Y-%%m-%%d 23:59:59'))]"/>
                </xpath>
            </field>
        </record>

        

    </data>
</odoo>

